import { render } from "@testing-library/react"
import App from '../App'

describe ('Testing Welcome Page', () => {

    it('Checks if welcome is correctly loaded', () => {
        render(App)
        const books = getBy
    })

})